import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  title: "dark",
  colors: {
    primary: "#1a202c", //f7fafc
    secondary: "#f7fafc",
    tertiary: "#808080",
  },
});

export default theme;
